# Выпуск, подлинность и эксплуатация

[Главная](../../README.ru.md) · [English](../en/release-and-operations.md) · [Опубликованный выпуск](https://github.com/olegeklepikov-collab/ultra-deep-research/releases/tag/v0.41.0a1-r153-v22)

## Точная идентификация публикации

Тег: `v0.41.0a1-r153-v22` (**предварительный выпуск**). Публичные исходники Research задаются неизменяемым тегом; после получения тега полный коммит определяется командой `git rev-parse v0.41.0a1-r153-v22^{commit}`. Исходный коммит Foundation в его подписанном манифесте: `8dd99cca1755d2f7a6e8513b9e7a72bb23f52752`. Исходные файлы Foundation распространяются в `foundation.zip`; отдельный публичный репозиторий Foundation здесь не заявляется.

| Артефакт | Файл | SHA-256 |
|---|---|---|
| Общий ZIP | `hermes-local-release-r153-v22.zip` | См. ресурс выпуска `SHA256SUMS` |
| Research r153 | `research.zip` | `6fc52b5fd64a82f257277976be6ca30a9a96993e8d6e32abaef1d3166eede6ff` |
| Foundation v22 | `foundation.zip` | `a1f34369cedec311d79228365ffd94bf6b08a228a2ef50fd5d3a5ddfb31a8d50` |

Внешний ZIP содержит `research.zip`, `foundation.zip`, `foundation.zip.minisig`, `release-signing.pub`, `manifest.json`, `README.md` и каталог документации `documentation/` той же версии. **Подпись Minisign относится к Foundation.** Проверенный `bridge-lock.json` Foundation закрепляет хеш Research ZIP. Внешний архив проверяется по отдельному ресурсу выпуска `SHA256SUMS`. Манифест описывает компоненты и снимок документации внутри поставки, а не изменяемую документацию в `main`.

Публикуются только открытые материалы выпуска. Рабочие базы, закрытые ключи, OAuth-токены, реквизиты шлюза, частные журналы и домашние каталоги оператора исключены. Изменение этой документации не изменяет подписанные архивы или тег выпуска.

## Что проверено

| Уровень | Свидетельства и предел вывода |
|---|---|
| Упаковка | Точный состав исполняемых файлов, хеши, объявленная лицензия, чистая исходная редакция Foundation и согласованная привязка Research |
| Изменённое поведение | Проверки маршрута класса модели в r153; ограничения маршрута, планирование Search, короткие обозначения и выбор источника унаследованы от r152 |
| Установка | Публичные точки входа, точные установленные байты, doctor; сохранены применимые свидетельства обновления и отката |
| Telegram и шлюз | Контрольные реальные приём и отправка, отказ второму владельцу, обновление отметки работоспособности и перезапуск в отдельной локальной среде |
| Восстановление | Копирование, восстановление и обратное чтение двенадцати классов; отдельные пределы по системам; превышение одной системы блокирует результат |
| Подлинность | Minisign и штатный `foundation_release_verify`; `foundation_bridge_health` вернул `integration_ready` |
| Прежний контроль Search r152 | Штатный `insufficient_evidence` без выпущенных утверждений; наследуемое свидетельство, не научный эталон r153 |

Прежние реестры содержат 44 ограниченных исполнения E2E и 862 нормативные позиции. Эти числа не означают 44 неограниченных живых запуска или 862 независимые научные приёмки. Каждое наблюдение сохраняет версию и область. Подготовленный набор из 20 FAIR-задач не завершён и не является общей оценкой точности выпуска.

## Изменения r153 / v22

- Research r153 добавляет настраиваемые оператором классы моделей с привязкой фактического маршрута поставщик/модель/рассуждение и сохранением отпечатка настроек. Название класса не подтверждает качество модели или исследовательского вывода.
- Foundation v22 закрепляет точный архив r153. Общий ZIP содержит снимок документации `documentation/` той же версии; хеш внешнего ZIP публикуется отдельно в `SHA256SUMS`.
- Исправления r152 для ограниченного маршрута, планирования Search, коротких обозначений и выбора источника сохранены. Это наследуемое поведение, а не новые научные результаты r153.

## Значение состояний

Таблица объясняет границы; точный перечень допустимых значений определяется схемой инструмента.

| Состояние или флаг | Практическое значение |
|---|---|
| `configured` / `available` | Есть настройка или точка входа; успешная квалификация из этого не следует |
| `verified` | Именованная проверка прошла для конкретного входа и области |
| `partial` / `review_required` | Работа сохранена, но обязательства не завершены либо нужна проверка |
| `insufficient_evidence` | Доступные материалы не обосновывают запрошенный вывод |
| `unknown_outcome` | Внешний эффект мог состояться; перед повтором нужна сверка |
| `integration_ready` | Проверки программной интеграции пройдены в наблюдённой среде |
| `qualified` | Решение относится только к конкретному квалифицированному профилю и области |
| `production_activation_allowed=false` | Подпись или успешная интеграция не разрешили производственную активацию |

## Восстановление и эксплуатационная политика

На проверочном стенде были приняты потеря изменений до 24 часов и восстановление до одного часа отдельно для двенадцати классов: профили и состояние шлюза; Beads; базы Dolt; полномочия и ограничения веток Dolt; SQLite исполнения; AgentMemory; Graphiti/FalkorDB; исходные данные и записи перестроения Zvec; артефакты; репозитории Git; закреплённые зависимости; конфигурационные манифесты.

Управляемое оператором задание было настроено каждые 12 часов, сохраняло три автоматических снимка, отклоняло параллельный запуск и считало состояние старше 24 часов устаревшим. Ручные снимки сохранялись. После изменения сборок повторялось восстановление; изменение настроек также отменяло актуальность привязки среды. Подтверждены первый фактический запуск службы и проверки просрочки/параллелизма; длительная гарантия непрерывной эксплуатации не устанавливалась.

**Это локальное задание относится к проверочному стенду и не устанавливается автоматически шестью файлами опубликованной поставки.** Foundation предоставляет контракты состава копии, целей и оценки восстановления. Развёртывание должно обеспечить собственные исполнение резервирования, расписание, хранение и контроль по `RECOVERY.md` компонента. Состояние `enabled` не доказывает наличие свежей успешной копии.

Секреты оператора хранятся вне Git. Реквизиты нового экземпляра предоставляются явно. Если прежний шлюз уже опрашивает Telegram-бота, второй экземпляр становится конкурирующим потребителем; нужен единственный владелец и явная передача управления. Прежняя память, задания и статусы приёмки не импортируются незаметно.

## Разбор неполадок

| Признак | Что проверить сначала |
|---|---|
| `hermes_python_entrypoint_required` | Python сценария и Python-точка `venv/bin/hermes` должны относиться к одному окружению |
| `model_route_not_allowed` | Поддержанную пару поставщика и модели в настройках выбранного домашнего каталога |
| Изменилась среда или конфигурация | Сохранить результаты; после намеренной перенастройки начать новый ограниченный запуск |
| `unsafe_public_text` | Путь поля: причиной могут быть длина или управляющий символ, а не обнаруженная утечка секрета |
| `unknown_outcome`, требуется сверка | Сохранить попытку и проверить действие/обратное чтение до нового внешнего эффекта |
| Telegram `send_path_degraded` сразу после подключения | Дождаться подтверждённого продвижения опроса: подключение ещё не означает готовность отправки |
| Неверная подпись выпуска | Сверить точные архив, подпись и доверенный ключ; прежняя подпись не удостоверяет новые байты |
| Недостаточно доказательств или профиль не квалифицирован | Проверить пробелы источников, чтения и проверки; не подгонять пороги под полученный ответ |

## Готовность не означает автоматическую активацию

Программная публикация завершена. Развёртывание, решение оператора об активации и квалификация исследовательских профилей раздельны. Это соответствует разграничению E2E-38, TC-214 и NFR-E32: ограниченная интеграция не наследует научную квалификацию. Выпуск предоставляет исполняемые контракты и явно ограниченные процессы, но не удостоверяет все исследовательские области и настройки поставщиков.
